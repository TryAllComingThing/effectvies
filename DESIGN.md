

本文档是信息管理平台前端开发的**唯一视觉真理源（Source of Truth）**。所有前端页面开发（Vue3 + Element Plus）**必须强制遵循本设计规范**，以确保高度还原 Axure 原型，实现页面风格统一、组件复用高效。

## 1. 视觉主题与整体氛围 (Visual Theme & Atmosphere)
- **整体风格**：专业、严谨的企业级后台管理平台。
- **设计哲学**：以数据展示为核心，强调信息层级清晰、操作高效、视觉干扰最小化。
- **色彩心理**：主色调采用科技蓝（#1677FF），传递信任与高效；配合中性灰背景，降低长时间操作的视觉疲劳。
- **还原要求**：严格对标 Axure 原型（登录页、权限管理、用户管理），实现零偏差还原。

## 2. 色彩系统与语义角色 (Color Palette & Roles)
开发时禁止硬编码 Hex 值，必须映射为以下语义变量（建议在 `theme.scss` 中定义）：

### 2.1 功能主色
- `--color-primary`: `#1677FF`（平台主题色，用于主按钮、选中态、高亮、搜索框聚焦）
- `--color-success`: `#00B42A`（成功状态，如启用标签）
- `--color-warning`: `#FF7D00`（警告提示）
- `--color-danger`: `#F53F3F`（危险操作，如删除按钮、停用标签、表单校验报错）
- `--color-disabled`: `#9CA3AF`（禁用置灰）

### 2.2 文字层级色
- `--text-primary`: `#1D2129`（一级标题、重要数据）
- `--text-regular`: `#4E5969`（二级正文、常规内容）
- `--text-secondary`: `#86909C`（三级辅助文字、备注）
- `--text-table-header`: `#333333`（表格表头文字）

### 2.3 边框与分割
- `--border-color`: `#E5E6EB`（通用边框）
- `--divider-color`: `#EEEEEE`（分割线）

### 2.4 侧边栏专属色彩
- `--sidebar-bg`: `#091E3F`（深色侧边栏背景）
- `--sidebar-text-default`: `#C0C6CF`（默认菜单文字）
- `--sidebar-text-active`: `#FFFFFF`（选中菜单文字）
- `--sidebar-bg-active`: `#1890FF`（选中菜单背景）

## 3. 字体排版规范 (Typography Rules)
- **全局字体栈**：`'Microsoft YaHei', 'PingFang SC', sans-serif`
- **行高基准**：`1.5`

| 层级名称 | 字号 (Font Size) | 字重 (Font Weight) | 应用场景 |
| :--- | :--- | :--- | :--- |
| `--font-size-h1` | 18px | 600 | 页面大标题 |
| `--font-size-h2` | 16px | 500 | 模块/卡片小标题 |
| `--font-size-body` | 14px | 400 | 正文常规、表单内容 |
| `--font-size-caption`| 12px | 400 | 备注、辅助文字、标签 |

## 4. 布局与栅格系统 (Layout Principles)
### 4.1 整体后台布局
- **结构**：左侧固定侧边栏 + 顶部固定导航栏 + 右侧自适应主内容区。
- **尺寸约束**：
    - 侧边栏宽度：`220px`
    - 顶部导航栏高度：`60px`
    - 主内容区最小宽度：`1200px`
    - 页面整体背景色：`#F5F7FA`
- **间距规范**：
    - 内容容器内边距：`20px`
    - 页面模块间距：`20px`

### 4.2 卡片容器规范
所有数据区块必须包裹在卡片中：
- 背景色：`#FFFFFF`
- 圆角：`4px`
- 阴影：`0 2px 12px 0 rgba(0, 0, 0, 0.06)`
- 内边距：`16px`

### 4.3 8px 基础栅格间距
- `--spacing-xs`: 4px (极小间距)
- `--spacing-sm`: 8px (基础间距、按钮间距)
- `--spacing-md`: 12px (中等间距、表格单元格内边距)
- `--spacing-lg`: 16px (标准间距、卡片内边距、表单项间距)
- `--spacing-xl`: 20px (大间距、页面模块间距)
- `--spacing-2xl`: 24px (超大间距)

## 5. 组件样式规范 (Component Stylings)
### 5.1 按钮组件 (Buttons)
- **尺寸**：高度 `36px`，左右内边距 `16px`。
- **类型样式**：
    - **主要按钮**（新增、确定）：背景 `--color-primary`，文字白色。
    - **次要按钮**（编辑、查看）：边框 `--color-primary`，文字 `--color-primary`，背景白色。
    - **危险按钮**（删除）：文字/边框 `--color-danger`。
    - **取消按钮**：白底，灰边框，灰色文字。
- **行内操作**：文字型按钮，间距 `8px`，仅展示“查看、编辑、删除”。

### 5.2 数据表格 (Tables) - 核心组件
- **基础样式**：
    - 头部背景：`#F7F8FA`，文字 `--text-table-header`。
    - 行高：`48px`。
    - 边框：完整边框模式，颜色 `--border-color`。
    - 单元格内边距：`12px 10px`。
    - 交互：隔行无特殊底色，鼠标悬浮高亮。
- **状态标签**：
    - 正常/启用：绿色背景 (`--color-success`)，白色文字，圆角 `4px`，内边距 `2px 8px`。
    - 禁用/停用：红色背景 (`--color-danger`)，白色文字，圆角 `4px`。
- **时间格式**：统一展示为 `YYYY-MM-DD HH:mm:ss`。

### 5.3 搜索筛选栏 (Search Bar)
- **布局**：左对齐表单项，右对齐操作按钮。
- **尺寸**：输入框、下拉框统一高度 `36px`。
- **排布**：单行排布，表单项间距 `16px`，默认嵌入白色卡片内。

### 5.4 表单弹窗 (Modals & Forms)
- **弹窗尺寸**：宽度 `650px`。
- **表单布局**：
    - 标签固定宽度：`90px`。
    - 输入框统一宽度：`320px`。
    - 表单项垂直间距：`18px`。
    - 底部按钮：居右排布。
- **校验规范**：必填项红色 `*` 前置；错误提示文字颜色 `--color-danger`，字号 `12px`。

### 5.5 登录页专属 (Login Page)
- **布局**：全屏居中。
- **表单盒子**：宽度 `380px`，居中显示。
- **主按钮**：通栏排布。
- **背景**：简洁浅色风格。

## 6. 深度与层级 (Depth & Elevation)
- **卡片层级**：`0 2px 12px 0 rgba(0, 0, 0, 0.06)`
- **弹窗层级**：遵循 Element Plus 默认遮罩层级。
- **下拉菜单/气泡**：需高于内容区层级，带轻微阴影。

## 7. 交互行为与权限 (Interaction & Permissions)
### 7.1 交互行为令牌
- **删除操作**：强制触发二次弹窗确认（使用 `ElMessageBox`）。
- **状态切换**：点击弹出确认提示后再变更。
- **空状态**：列表无数据时，统一展示空状态图文。
- **加载状态**：接口请求时触发全局统一 Loading 样式。
- **反馈提示**：操作成功/失败统一使用全局 Message 消息提示。


## 9. AI 代理开发指南 (Agent Prompt Guide)
**致 Codex / AI 开发者：**
1. **禁止硬编码**：所有颜色、尺寸、间距必须引用本文档定义的语义变量或 SCSS 变量。
2. **组件复用**：公共表格、搜索栏、弹窗必须封装为业务组件（`components/business/`），直接引用本令牌样式。
3. **原型还原**：生成代码时，优先保证与 Axure 原型在布局、字段顺序、交互逻辑上的一致性。
4. **权限控制**：所有操作按钮必须通过 `v-permission` 指令或全局权限数组进行动态渲染控制。
5. **开发顺序**：新页面开发时，先对照本文档核对视觉规范，再编写具体的业务逻辑代码。
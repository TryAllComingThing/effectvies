

1. 页面框架
  1.1 侧边栏菜单：支持侧边栏的收起与展开；菜单+子菜单（点击菜单：展开子菜单或者收起子菜单）；菜单前具有合适的icon
  1.2 Header： 左侧Logo + 平台名称， 右侧 - 国际化（中文/英文）、主题色切换、消息（点击展开详情）、avater + 账户名称（点击下拉菜单：个人信息、修改密码、退出系统）
  1.3 其他区域：参考 ruoyi框架的前端设计
 
2. 功能清单：
  2.1. 绩效文件管理
    2.1.1 文件管理
       - 支持文件上传（弹窗本地上传），格式excel\压缩包；上传文件本地化存储
       - 列表显示（多选框、序号、批号、名称、格式、账户、日期、操作 - 解析、任务、编辑、删除、覆盖、日志）
         a. 批号自动生成：JXData-名称（提取字符4~6）-日期（数字）
         b. 解析：点击执行解析（状态跟踪）
         c. 生成任务：用于执行匹配任务（创建任务到任务管理）
         d. 日志：解析汇总 + 解析列表
         e. 覆盖：上传新的文件覆盖旧的文件，同时清除日志
       - 功能按钮：新增、删除
    2.1.2.  任务管理
       - 功能按钮：新增、删除
       - 列表显示 (多选框、序号、任务号、批号、文件名称、日期、状态、操作 - 开启/停止、删除、日志、清除执行)
         a. 任务号自动生成： Task-名称（提取字符4~6）-日期（数字）
         b. 开启/停止：开始执行智能匹配，并生成匹配列表(支持继续)
         c. 日志：显示任务执行结果汇总，以及匹配出来的结果
         d. 清除执行：清除该批次、该任务生成的所有匹配结果（匹配结果会自动添加到绩效的数据库表）
  2.2. 绩效管理
    2.2.1  绩效审核管理
       - 功能按钮： 新增、修改、删除
       - 多维搜索区域：路线、科室、提出人、时间等多维度组合查询，支持模糊搜索和高级筛选
       - 绩效列表（多选框、序号、主题、内容、路线、科室、提出人、分数、时间、解析时间、置信度、操作-修改、删除、审核、详情）
         a.详情：任务号-批次、匹配时间；匹配的具体源信息
    2.2.2 绩效管理
       - 功能按钮： 新增、修改、删除
       - 多维搜索区域：路线、科室、提出人、时间等多维度组合查询，支持模糊搜索和高级筛选
       - 绩效列表（多选框、序号、主题、内容、路线、科室、提出人、分数、时间、解析时间、置信度、操作-修改、删除、详情）
         a.详情：任务号-批次、匹配时间；匹配的具体源信息
  2.3. 绩效分析
    2.3.1 绩效报表
       - 日常统计报表，已表格形式展示，支持报表导出下载
       - 支持报告的多维过滤筛选：按科室、路线、时间、提出人
    2.3.3 绩效报告
       - 日报：基于模板开发word类的分析报告；提供日期筛选、科室筛选、路线筛选等；提供导出word下载
       - 季度报：基于模板开发word类的分析报告；提供季度筛选、科室筛选、路线筛选等；提供导出word下载
       - 年报：基于模板开发word类的分析报告；提供年筛选、科室筛选、路线筛选等；提供导出word下载
       - 当前报告：基于模板开发word类的分析报告；提供时间范围筛选、科室筛选、路线筛选等；提供导出word下载
  2.4. 系统管理
    2.4.1 用户管理
       - 基础结构：页面顶部配置多条件筛选搜索区，支持账号、姓名、科室、角色、账号状态、创建时间范围多维检索，搭配重置、搜索快捷操作按钮
       - 列表字段：多选框、序号、用户账号、用户姓名、所属科室、所属角色、联系电话、账号状态、创建人、创建时间、最后登录时间、操作栏
       - 支持批量操作：批量启用、批量禁用、批量删除用户数据
       - 单条操作权限：查看详情、编辑用户信息、重置登录密码、启用 / 禁用账号、删除用户
       - 弹窗表单：新增 / 编辑用户支持填写账号、姓名、手机号、归属科室、分配角色、设置账号状态、填写备注信息
       - 配套功能：用户状态启停管控、登录权限管控、个人信息维护、账号数据分页展示、数据导出
    2.4.2 角色管理
       - 角色类型：管理员和普通用户 【普通用户，不具备绩效管理的操作、绩效文件管理的操作、以及系统管理的操作】
       - 功能：新增、删除
       - 列表（多选框、序号、角色编码、角色名称、科室、创建者、日期、操作 - 查看、编辑、删除）
       - 数据管控：禁止删除已绑定用户的角色，避免权限体系错乱
       - 权限配置：支持给对应角色分配页面菜单权限、按钮操作权限，精准管控后台访问与操作范围 
    2.4.3 科室管理
       - 页面布局：顶部设置科室名称、上级科室快速搜索栏，支持快速定位层级科室
       - 数据结构：采用树形层级结构展示科室架构，支持一级科室、二级子科室多级划分
       - 列表字段：多选框、序号、科室编码、科室名称、上级科室、排序序号、科室状态、创建人、创建时间、操作栏
       - 核心功能：新增顶级科室、新增下级子科室、编辑科室信息、调整科室排序、启用 / 停用科室、删除空置科室
       - 业务关联：绑定用户、角色、规则数据归属关系，所有业务数据可精准归属对应科室
    2.4.4 规则管理
       - 功能：新增、删除
       - 多维搜索区域：科室、标签、名字/内容、时间
       - 列表（多选框、序号、规则名称、标签、规则内容、科室、创建者、日期、操作 - 查看、编辑、删除、启用）
    2.4.5 语义字典管理
       - 页面结构：采用 Tabs 标签页切换，包含两个核心模块
           Tabs1：业务语义定义
           Tabs2：SQL 模板定义
       - Tabs1：业务语义定义
         用途：统一管理平台业务术语、关键词、标准语义、字段释义，用于规则解析、接口匹配、文本处理
         搜索区域：支持语义编码、语义名称、关键词、所属科室、状态、创建时间检索
         列表字段：多选框、序号、语义编码、语义名称、关键词、匹配规则、归属科室、状态、创建者、创建时间、操作
         功能：新增、编辑、删除、启用 / 停用、批量操作
         表单字段：语义编码、语义名称、关键词、匹配类型、科室、状态、备注、内容定义
      - Tabs2：SQL 模板定义
        用途：管理系统通用 SQL 模板、查询语句、统计脚本、数据抽取模板，支持动态参数替换
        搜索区域：支持模板编码、模板名称、SQL 关键词、所属科室、状态检索
        列表字段：多选框、序号、模板编码、模板名称、SQL 简述、参数个数、科室、创建者、创建时间、操作
        功能：新增、编辑、删除、启用 / 停用、SQL 在线预览、执行测试
        表单字段：模板编码、模板名称、SQL 内容、动态参数、科室、状态、备注
        安全控制：SQL 语法校验、敏感关键字拦截、预览不执行、仅管理员可维护
    2.4.6 数据字典管理
          - 参考标准：完全参照 Ruoyi 框架数据字典设计规范开发，统一后台字典数据管理逻辑
          - 层级划分：分为字典类型与字典数据两级管理
          - 默认字典类型： 路线、类型等
          - 字典类型管理：维护系统全局字典大类，包含字典编码、字典名称、字典状态、备注、创建时间，支持新增、编辑、启用禁用、删除、查看详情
          - 字典数据管理：绑定对应字典类型，维护字典具体键值对，包含字典标签、字典键值、排序、样式类、状态等字段
          - 检索能力：支持字典名称、字典编码、状态快速筛选查询
          - 配套功能：字典数据批量新增、字典缓存刷新、字典数据导入导出、字典状态统一管控
          - 业务作用：统一系统下拉选项、状态枚举、分类标识、通用固定值，全平台页面下拉框、状态选择直接调用字典数据，统一前端展示与后端存储值
    2.4.7 日志管理
          - 功能按钮： 批量删除
          - 提供登录日志页签， 列表（序号、操作者、IP、科室、时间、操作 - 删除）；筛选：科室、IP、时间范围
          - 操作日志页签，列表（序号、模块、操作描述、操作类型、操作者、科室、时间、操作 - 删除）；筛选： 科室、模块、时间范围

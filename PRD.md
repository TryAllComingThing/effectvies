# 绩效信息管理平台 PRD 需求规格说明书

## 1. 文档信息
- 文档名称：绩效信息管理平台 PRD
- 文档版本：v1.1
- 修订日期：2026-05-17
- 适用对象：产品、前端研发、后端研发、测试、实施运维
- 需求来源：`Functions.md`
- 修订说明：补充状态机、审核流边界、RBAC权限矩阵、API契约、导出规范、测试矩阵、里程碑门禁。

## 2. 项目背景与目标
- 背景：当前绩效数据来源分散、规则不统一、审核链路不闭环，需建设统一平台完成“文件导入 -> 解析匹配 -> 审核管理 -> 报表报告 -> 系统治理”的全流程管理。
- 目标：
  1. 建立可追溯的绩效数据处理闭环。
  2. 支持按科室/路线/人员维度进行高效检索、分析、导出。
  3. 建立清晰权限体系（菜单+按钮+数据范围），满足企业级合规与管控要求。

## 3. 产品范围
### 3.1 本期范围（In Scope）
- 页面框架：侧边栏、Header、消息中心入口、主题/语言切换、账号下拉菜单。
- 绩效文件管理：文件管理、任务管理。
- 绩效管理：绩效审核管理、绩效管理。
- 绩效分析：绩效报表、绩效报告（日/季/年/当前）。
- 系统管理：用户、角色、科室、规则、语义字典、数据字典、日志管理。

### 3.2 非本期范围（Out of Scope）
- 智能解析算法本身优化（仅定义触发、状态、结果展示）。
- 第三方消息通道（短信/邮件/IM）推送。
- 多租户隔离与计费。
- 后端接口开发、数据库设计与服务部署（本期不包含）。

## 4. 用户角色与权限
- 管理员：拥有全部菜单和按钮权限，可维护系统配置、字典、规则、SQL模板。
- 普通用户：
  - 不具备：绩效管理操作权限、绩效文件管理操作权限、系统管理操作权限。
  - 具备：授权范围内查询、查看详情、报表查看与导出（按角色配置）。
- 权限粒度：
  1. 菜单级：可见/不可见。
  2. 按钮级：新增、编辑、删除、启用、导出等操作权限控制。
  3. 数据级：按科室归属限制可访问数据范围（仅本人科室、含下级科室、全量）。

## 5. 全局交互与界面规范
- 侧边栏：支持收起/展开；一级菜单点击后展开/收起子菜单；菜单带 icon。
- Header：左侧 Logo+平台名；右侧含语言切换（中/英）、主题切换、消息、头像下拉（个人信息/修改密码/退出）。
- 列表页统一规范：顶部筛选区 + 操作按钮区 + 表格区 + 分页区。
- 删除类操作：需二次确认。
- 上传、解析、执行任务等异步操作：展示状态（进行中/成功/失败）与日志入口。

## 6. 业务流程（端到端）
1. 用户上传绩效文件（Excel/压缩包），系统生成批号。
2. 触发文件解析，记录解析汇总与明细日志。
3. 基于文件创建匹配任务，系统生成任务号。
4. 启动任务执行智能匹配，生成匹配结果并入绩效待审核池。
5. 在绩效审核管理中完成审核（通过/驳回），通过后进入绩效管理正式库。
6. 基于正式库数据输出报表和报告（Word/表格导出）。

## 7. 功能需求明细
### 7.1 页面框架
#### 7.1.1 侧边栏菜单
- 功能：收起/展开、菜单层级展示、菜单 icon。
- 验收标准：
  1. 点击一级菜单可展开/收起子菜单。
  2. 收起后仅显示 icon，悬浮可见菜单名称。

#### 7.1.2 Header
- 功能：语言切换、主题切换、消息详情展开、账号下拉菜单。
- 验收标准：
  1. 语言切换后 UI 文案即时切换并持久化。
  2. 退出系统后清理登录态并跳转登录页。

### 7.2 绩效文件管理
#### 7.2.1 文件管理
- 输入：本地文件（`xls/xlsx/zip`）。
- 操作：新增、删除、解析、生成任务、编辑、删除、覆盖、日志。
- 列表字段：多选框、序号、批号、名称、格式、账户、日期、操作。
- 业务规则：
  1. 批号自动生成：`JXData-{yyyyMMdd}-{8位随机码}`，全局唯一。
  2. 覆盖上传后，原文件解析日志清空，并写入覆盖审计记录。
  3. 删除文件前校验：若存在“运行中任务”则禁止删除。
  4. 上传容量：单文件 <= 200MB；单次最多10文件；zip解压层级<=3。

#### 7.2.2 任务管理
- 操作：新增、删除、开启/停止、日志、清除执行。
- 列表字段：多选框、序号、任务号、批号、文件名称、日期、状态、操作。
- 业务规则：
  1. 任务号自动生成：`Task-{yyyyMMdd}-{8位随机码}`，全局唯一。
  2. 开启后执行匹配并持续产出结果；停止后可继续，继续从最近成功检查点恢复。
  3. 清除执行会删除该任务产出的全部匹配结果，并写审计日志。

### 7.3 绩效管理
#### 7.3.1 绩效审核管理
- 操作：新增、修改、删除、审核、详情。
- 筛选：路线、科室、提出人、时间，支持模糊与高级筛选。
- 列表字段：多选框、序号、主题、内容、路线、科室、提出人、分数、时间、解析时间、置信度、审核状态、操作。
- 详情字段：任务号、批次号、匹配时间、源信息明细、审核记录。
- 审核规则：
  1. 审核动作：通过、驳回。
  2. 审核字段：审核人、审核时间、审核意见（驳回必填）。
  3. 通过后流入绩效管理正式库；驳回保留在审核池并标记驳回原因。

#### 7.3.2 绩效管理
- 操作：新增、修改、删除、详情。
- 筛选与列表字段同审核管理（不含审核操作）。
- 数据来源：仅展示审核通过的数据。

### 7.4 绩效分析
#### 7.4.1 绩效报表
- 功能：按科室/路线/时间/提出人过滤，表格展示，导出下载。
- 导出规则：
  1. 文件命名：`绩效报表_{yyyyMMddHHmmss}.xlsx`。
  2. 导出字段顺序与页面列顺序一致。
  3. 时区统一：`Asia/Shanghai`。

#### 7.4.2 绩效报告
- 类型：日报、季度报、年报、当前报告。
- 功能：基于模板生成 Word 报告并导出。
- 过滤：
  1. 日报：日期+科室+路线。
  2. 季报：季度+科室+路线。
  3. 年报：年份+科室+路线。
  4. 当前报告：时间范围+科室+路线。
- 导出规则：
  1. 文件命名：`绩效报告_{类型}_{yyyyMMddHHmmss}.docx`。
  2. 异步导出：超过10秒转异步任务，支持进度查询与失败重试。

### 7.5 系统管理
#### 7.5.1 用户管理
- 筛选：账号、姓名、科室、角色、状态、创建时间范围。
- 列表字段：多选框、序号、账号、姓名、科室、角色、电话、状态、创建人、创建时间、最后登录时间、操作。
- 功能：新增/编辑、重置密码、启用/禁用、删除、批量启用/禁用/删除、导出。

#### 7.5.2 角色管理
- 角色类型：管理员、普通用户。
- 功能：新增、删除、查看、编辑、权限分配。
- 规则：禁止删除已绑定用户的角色。

#### 7.5.3 科室管理
- 展示：树形层级（多级科室）。
- 功能：新增顶级/下级、编辑、排序、启停、删除空置科室。
- 关联：用户、角色、规则等数据归属绑定科室。

#### 7.5.4 规则管理
- 筛选：科室、标签、名称/内容、时间。
- 列表：规则名称、标签、内容、科室、创建者、日期、操作。
- 功能：新增、删除、查看、编辑、启用。

#### 7.5.5 语义字典管理
- Tabs1 业务语义定义：支持检索、新增、编辑、删除、启停、批量操作。
- Tabs2 SQL模板定义：支持检索、新增、编辑、删除、启停、SQL预览、执行测试。
- 安全控制：
  1. 仅管理员可维护。
  2. 执行测试仅允许 `SELECT`。
  3. 强制 `LIMIT 1000`。
  4. 超时上限10秒，超时自动终止。
  5. 敏感关键字（DDL/DML）拦截。

#### 7.5.6 数据字典管理
- 双层结构：字典类型 + 字典数据。
- 默认类型：路线、类型等。
- 功能：新增、编辑、启停、删除、导入导出、缓存刷新、批量新增。

#### 7.5.7 日志管理
- 页签：登录日志、操作日志。
- 功能：单条删除、批量删除。
- 筛选：
  1. 登录日志：科室、IP、时间范围。
  2. 操作日志：科室、模块、时间范围。
- 合规策略：
  1. 保留周期：登录日志180天、操作日志365天。
  2. 敏感字段脱敏：手机号、IP部分掩码。

## 8. 关键数据对象
- 用户 User：id、account、name、deptId、roleIds、status、phone、lastLoginAt。
- 角色 Role：id、code、name、deptId、permissions、status。
- 科室 Dept：id、code、name、parentId、sort、status。
- 文件 PerfFile：id、batchNo、name、format、owner、uploadedAt、parseStatus。
- 任务 PerfTask：id、taskNo、batchNo、fileId、status、startedAt、stoppedAt、checkpoint。
- 审核池 PerformanceReview：id、performanceId、reviewStatus、reviewer、reviewedAt、reviewComment。
- 绩效 Performance：id、title、content、routeCode、deptId、proposer、score、eventAt、parsedAt、confidence。
- 日志 Log：id、type、module、operator、ip、deptId、content、createdAt。

## 9. 状态机定义
### 9.1 文件解析状态
- 状态：`未解析 -> 解析中 -> 解析成功/解析失败`。
- 触发与约束：
  1. 未解析 -> 解析中：点击“解析”，文件存在且格式合法。
  2. 解析中 -> 解析成功：解析服务返回成功并写入结果。
  3. 解析中 -> 解析失败：解析异常或超时，记录失败原因。
  4. 解析中禁止删除与覆盖。

### 9.2 任务状态
- 状态：`未开始 -> 运行中 -> 已停止 -> 运行中 -> 已完成/失败`。
- 触发与约束：
  1. 未开始 -> 运行中：点击开启。
  2. 运行中 -> 已停止：点击停止，记录checkpoint。
  3. 已停止 -> 运行中：点击继续，从checkpoint恢复。
  4. 运行中 -> 已完成：全部数据匹配完成。
  5. 运行中 -> 失败：系统异常，允许重试。

### 9.3 审核状态
- 状态：`待审核 -> 审核通过/审核驳回`。
- 规则：
  1. 驳回必须填写意见。
  2. 通过后不可再编辑审核结论；如需变更走“反审核”流程（管理员权限）。

## 10. RBAC 权限矩阵（核心）
| 模块 | 操作 | 管理员 | 普通用户 |
|---|---|---|---|
| 绩效文件管理 | 查看 | Y | N |
| 绩效文件管理 | 新增/删除/解析/覆盖/生成任务 | Y | N |
| 任务管理 | 开启/停止/清除执行 | Y | N |
| 绩效审核管理 | 审核 | Y | N |
| 绩效管理 | 新增/修改/删除 | Y | N |
| 绩效分析 | 查看 | Y | Y（按授权） |
| 绩效分析 | 导出 | Y | Y（按授权） |
| 系统管理 | 用户/角色/科室/规则/字典维护 | Y | N |
| 日志管理 | 查看/删除 | Y | N |

- 生效规则：默认拒绝、显式授权。
- 生效时机：权限变更后重新登录生效（v1.1）。

## 11. 前端Mock交互规范（替代后端接口）
- 本期目标：仅前端页面开发，所有数据交互使用Mock数据，不依赖真实后端。
- Mock响应结构：`{ code, message, data, traceId }`。
- Mock错误码：`0=成功, 400xx=参数错误, 401xx=鉴权失败, 403xx=越权, 409xx=状态冲突, 500xx=系统异常`。
- Mock实现建议：`vite-plugin-mock` 或 `msw`，按模块拆分mock文件（文件管理、任务、审核、报表、系统管理）。

### 11.1 文件管理（Mock API）
1. `POST /api/perf/files/upload`：上传文件。
2. `POST /api/perf/files/{fileId}/parse`：触发解析。
3. `POST /api/perf/files/{fileId}/cover`：覆盖文件。
4. `DELETE /api/perf/files/{fileId}`：删除文件。
5. `GET /api/perf/files`：分页查询。

### 11.2 任务管理（Mock API）
1. `POST /api/perf/tasks`：创建任务。
2. `POST /api/perf/tasks/{taskId}/start`：开启任务。
3. `POST /api/perf/tasks/{taskId}/stop`：停止任务。
4. `POST /api/perf/tasks/{taskId}/resume`：继续任务。
5. `POST /api/perf/tasks/{taskId}/clear`：清除执行结果。
6. `GET /api/perf/tasks`：分页查询。

### 11.3 审核与绩效（Mock API）
1. `GET /api/perf/reviews`：审核池分页查询。
2. `POST /api/perf/reviews/{id}/approve`：审核通过。
3. `POST /api/perf/reviews/{id}/reject`：审核驳回。
4. `GET /api/performance`：正式绩效分页查询。

### 11.4 导出（Mock API）
1. `POST /api/reports/export/table`：创建报表导出任务。
2. `POST /api/reports/export/docx`：创建报告导出任务。
3. `GET /api/reports/export/{jobId}/status`：查询进度。
4. `GET /api/reports/export/{jobId}/download`：下载文件。

- 幂等要求：前端需实现按钮防重与请求节流，模拟 `Idempotency-Key` 行为。
- 分页规范：`pageNum/pageSize/total/list`。
- Mock数据要求：
  1. 每个模块不少于50条列表数据（含分页场景）。
  2. 必须覆盖空数据、异常数据、长文本、边界时间、禁用状态。
  3. 状态流转通过前端本地状态机或Mock状态存储实现。

## 12. 非功能需求（前端）
- 性能：
  1. 页面首屏加载（本地开发环境）<= 3s。
  2. 列表分页切换与筛选响应（Mock场景）<= 500ms。
  3. 大表格（1000条前端渲染）无明显卡顿。
- 安全：
  1. 所有接口鉴权；关键操作记录审计日志。
  2. SQL模板执行测试必须权限校验+关键字拦截。
- 可用性：
  1. 异常需可读提示，不得白屏。
  2. 长任务需可中断、可恢复。

## 13. 验收标准（UAT，前端范围）
1. 普通用户登录后不可见“绩效文件管理、绩效管理、系统管理”入口。
2. 文件上传后自动生成批号，解析流程可追踪且日志可查看。
3. 任务支持开启/停止/继续，停止后继续不丢进度。
4. 审核管理支持通过/驳回，驳回必填意见；通过后进入正式库。
5. 报表可按条件过滤并导出；报告可按模板生成 Word 并下载。
6. 角色删除受绑定校验约束；字典、规则、语义、日志管理功能完整可用。
7. 所有页面在Mock模式下可完整演示主流程、异常流程与权限差异。
8. P0缺陷为0，P1缺陷关闭率100%后方可提测。

## 14. 测试矩阵（新增）
| 模块 | 功能测试 | 异常测试 | 安全测试 | 自动化优先级 |
|---|---|---|---|---|
| 文件管理 | 上传/解析/覆盖/删除 | 大文件超时/重复上传 | 越权上传/删除 | P0 |
| 任务管理 | 开启/停止/继续/清除 | 中断恢复/并发点击 | 越权控制任务 | P0 |
| 审核管理 | 通过/驳回/详情 | 驳回意见缺失 | 越权审核 | P0 |
| 报表报告 | 过滤/导出/下载 | 导出失败重试 | 越权导出 | P1 |
| 用户角色科室 | 增删改查/绑定关系 | 角色删除冲突 | 权限绕过 | P0 |
| 字典与规则 | 启停/批量/缓存刷新 | 脏数据导入 | SQL关键字拦截 | P1 |
| 日志管理 | 查询/删除/筛选 | 大量日志分页 | 敏感字段脱敏校验 | P1 |

## 15. 里程碑与门禁（前端）
1. M1（1周）：框架+权限+基础字典+Mock基建。
- 进入条件：前端项目脚手架初始化完成。
- 退出条件：登录、菜单、按钮权限在Mock模式可验收通过。
2. M2（2周）：文件管理+任务管理+日志链路（Mock交互）。
- 进入条件：M1通过。
- 退出条件：上传、解析、任务状态机、日志链路在Mock模式通过测试。
3. M3（2周）：审核管理+绩效管理+分析报表（Mock交互）。
- 进入条件：M2通过。
- 退出条件：审核流、正式库展示、导出链路前端回归通过。
4. M4（1周）：报告导出+前端性能优化+UAT。
- 进入条件：全页面开发完成。
- 退出条件：性能达标、P0=0、UAT签字。

## 16. 风险与依赖
- 依赖：产品原型、前端UI资源、Mock数据方案。
- 风险：大文件上传与解析耗时、任务中断一致性、权限配置复杂导致误授权。
- 缓解：分片上传、任务断点续跑、权限变更审批与审计机制。

## 17. 术语表（新增）
- 批号：一次文件导入批次的唯一编号。
- 任务号：一次匹配任务执行的唯一编号。
- 审核池：待审核的匹配结果数据集合。
- 正式库：审核通过后可用于报表和报告的数据集合。
- 置信度：匹配引擎输出的结果可信评分。
- 当前报告：基于任意时间范围生成的阶段性报告。

## 18. Mock数据字典（前端执行版）
### 18.1 通用字段规范
- `id`：字符串，UUID或雪花ID字符串。
- `createdAt/updatedAt`：`yyyy-MM-dd HH:mm:ss`（`Asia/Shanghai`）。
- `status`：统一枚举，启用=`enabled`，停用=`disabled`。
- 分页响应：`{ list, total, pageNum, pageSize }`。

### 18.2 模块数据字典
#### 18.2.1 文件管理 `PerfFile`
- 字段：`id, batchNo, name, format, ownerName, uploadedAt, parseStatus`。
- `format`：`xls | xlsx | zip`。
- `parseStatus`：`unparsed | parsing | success | fail`。

#### 18.2.2 任务管理 `PerfTask`
- 字段：`id, taskNo, batchNo, fileName, createdAt, taskStatus, progress, checkpoint`。
- `taskStatus`：`pending | running | stopped | done | fail`。
- `progress`：`0-100`整数。

#### 18.2.3 审核池 `PerformanceReview`
- 字段：`id, title, content, routeName, deptName, proposer, score, eventAt, parsedAt, confidence, reviewStatus, reviewComment`。
- `reviewStatus`：`pending | approved | rejected`。
- `confidence`：`0.00-1.00`。

#### 18.2.4 正式绩效 `Performance`
- 字段：`id, title, content, routeName, deptName, proposer, score, eventAt, parsedAt, confidence, sourceTaskNo, sourceBatchNo, matchedAt`。

#### 18.2.5 用户与权限
- 用户 `User`：`id, account, name, deptId, deptName, roleCodes, phone, userStatus, lastLoginAt`。
- 角色 `Role`：`id, roleCode, roleName, dataScope, roleStatus`。
- `roleCode`：`admin | user`。
- `dataScope`：`self_dept | self_and_children | all`。

### 18.3 Mock场景数据覆盖要求
- 正常场景：每核心列表不少于50条。
- 边界场景：空列表、1条、整页临界值（10/20/50）。
- 异常场景：401未登录、403越权、409状态冲突、500系统异常。
- 文本场景：超长标题（>=120字符）、多行内容、特殊字符。
- 时间场景：跨月、跨季度、跨年数据。

## 19. 页面级交互脚本（Given/When/Then）
### 19.1 登录与权限
1. 管理员登录
- Given：账号角色为`admin`。
- When：登录成功进入首页。
- Then：可见全部一级菜单与授权按钮。
2. 普通用户登录
- Given：账号角色为`user`。
- When：登录成功进入首页。
- Then：不可见“绩效文件管理/绩效管理/系统管理”操作入口。

### 19.2 文件管理
1. 上传成功
- Given：选择`xlsx`文件且大小<=200MB。
- When：点击上传。
- Then：列表新增一条记录，`parseStatus=unparsed`，自动生成`batchNo`。
2. 触发解析
- Given：文件状态为`unparsed`。
- When：点击解析。
- Then：状态`unparsed->parsing->success`，日志可查看。
3. 覆盖文件
- Given：已有同名业务文件。
- When：点击覆盖并上传新文件。
- Then：原解析日志清空并新增覆盖审计记录。
4. 删除限制
- Given：该文件存在`running`任务。
- When：点击删除。
- Then：提示“存在运行中任务，禁止删除”，数据不变。

### 19.3 任务管理
1. 开启任务
- Given：任务状态`pending`。
- When：点击开启。
- Then：状态变更为`running`，`progress`持续增长。
2. 停止并继续
- Given：任务状态`running`且`progress>0`。
- When：点击停止后再点击继续。
- Then：状态`running->stopped->running`，从`checkpoint`继续。
3. 清除执行
- Given：任务已产生匹配结果。
- When：点击清除执行并确认。
- Then：结果清空，写入操作日志。

### 19.4 审核管理与绩效管理
1. 审核通过
- Given：审核池存在`pending`记录。
- When：点击审核通过。
- Then：记录从审核池移入正式绩效列表。
2. 审核驳回
- Given：审核池存在`pending`记录。
- When：点击驳回且填写意见。
- Then：状态改为`rejected`并保留驳回意见。
3. 驳回校验
- Given：审核弹窗未填写驳回意见。
- When：提交驳回。
- Then：前端阻断提交并提示“驳回意见必填”。

### 19.5 报表与报告
1. 报表导出
- Given：已选择筛选条件。
- When：点击导出报表。
- Then：生成文件名`绩效报表_{yyyyMMddHHmmss}.xlsx`的下载任务。
2. 报告异步导出
- Given：报告生成耗时>10秒（Mock延迟）。
- When：点击导出报告。
- Then：展示任务进度，可轮询至成功后下载`docx`。

### 19.6 系统管理
1. 角色删除拦截
- Given：角色已绑定用户。
- When：点击删除角色。
- Then：返回409冲突并提示“角色已绑定用户，不可删除”。
2. SQL模板执行测试拦截
- Given：SQL包含`UPDATE/DELETE`。
- When：点击执行测试。
- Then：前端展示敏感关键字拦截提示，不进入执行成功态。

### 19.7 日志管理
1. 登录日志筛选
- Given：存在不同科室/IP/时间日志。
- When：按科室+时间筛选。
- Then：仅显示命中数据，分页总数同步变化。
2. 批量删除日志
- Given：勾选多条日志。
- When：点击批量删除并确认。
- Then：列表移除选中数据并提示删除成功。
